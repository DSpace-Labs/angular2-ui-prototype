import {ObjectUtil} from "../../utilities/commons/object.util";


export class Date
{


    /**
     * DSpace expects ISO 8601, but might not always get it.
     * @param fromDC
     */
    constructor(fromDC)
    {
        console.log("original input: " + fromDC);


    }

}